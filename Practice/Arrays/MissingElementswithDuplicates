pair<int,int> missingAndRepeating(vector<int> &arr, int n)
{
	int rep=-1;
	for(int i=0; i<n; i++){
		
		int index=abs(arr[i]);

		if(arr[index-1]>0){
			arr[index-1] *= -1;
		}
		else{
			rep=index;
		}

		index++;
	}

	for(int i=0; i<n; i++){
		if(arr[i]>0){
			return {i+1, rep};
		}
	}

}